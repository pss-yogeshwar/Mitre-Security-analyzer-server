input {
  tcp {
    port => 24224
    codec => json
  }
}

filter {
  if [event] == "admin_login" and [status] == "failed" {
    mutate {
      add_field => { "attack_type" => "brute_force" }
    }
  }
}

output {
  stdout { codec => rubydebug }
}
